name: win_flutter
description: "Windows WebView project"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter
  webview_windows:
    git:
      url: https://github.com/MosioKix/webview_windows.git
      ref: main  # اگه شاخه‌ی فورک‌شده‌ات main نیست، بذار master یا همونی که هست

dev_dependencies:
  flutter_test:
    sdk: flutter
